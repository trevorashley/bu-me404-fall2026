[project]
name = "lecture-demos"
version = "0.1.0"
description = "Runnable demonstrations for the linear-ODEs-to-Laplace lecture"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "scipy>=1.14",
    "matplotlib>=3.9",
    "control>=0.10",
]
